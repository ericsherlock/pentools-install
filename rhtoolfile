# --- Core Pentesting Tools ---
NMAP (auto) dnf install -y nmap
WIRESHARK (auto) dnf install -y wireshark
AIRCRACK-NG (auto) dnf install -y aircrack-ng
JOHN-THE-RIPPER (auto) dnf install -y john
HYDRA (auto) dnf install -y hydra
HASHCAT (auto) dnf install -y hashcat
METASPLOIT-FRAMEWORK (auto) dnf install -y metasploit
SQLMAP (auto) dnf install -y sqlmap
NIKTO (auto) dnf install -y nikto
LYNIS (auto) dnf install -y lynis
OPENVAS (auto) dnf install -y openvas
ZAPROXY (auto) dnf install -y zaproxy
BURPSUITE (auto) dnf install -y burpsuite
RECON-NG (auto) dnf install -y recon-ng
SETOOLKIT (auto) dnf install -y setoolkit
WIFITE (auto) dnf install -y wifite
NETCAT (auto) dnf install -y nmap-ncat
MITMPROXY (auto) dnf install -y mitmproxy
CRUNCH (auto) dnf install -y crunch
FOREMOST (auto) dnf install -y foremost
BINWALK (auto) dnf install -y binwalk
CHKROOTKIT (auto) dnf install -y chkrootkit
MACCHANGER (auto) dnf install -y macchanger
DRIFTNET (auto) dnf install -y driftnet
CUTYCAPT (auto) dnf install -y CutyCapt
GOBUSTER (auto) dnf install -y gobuster
AMASS (auto) dnf install -y amass
SUBLIST3R (pip) pip3 install sublist3r
THEHARVESTER (pip) pip3 install theHarvester
DNSRECON (pip) pip3 install dnsrecon
FIERCE (pip) pip3 install fierce
DIRB (auto) dnf install -y dirb
DIRSEARCH (pip) pip3 install dirsearch
RESPONDER (manual) git clone https://github.com/SpiderLabs/Responder.git
EVIL-WINRM (pip) pip3 install evil-winrm
BLOODHOUND (manual) git clone https://github.com/BloodHoundAD/BloodHound.git
CRACKMAPEXEC (pip) pip3 install crackmapexec
IMPACKET (pip) pip3 install impacket
GIT (auto) dnf install -y git
PYTHON3-PIP (auto) dnf install -y python3-pip

# --- Wireless & Radio ---
KISMET (auto) dnf install -y kismet
REAVER (auto) dnf install -y reaver
COWPATTY (auto) dnf install -y cowpatty
GQRX (auto) dnf install -y gqrx
RTL-SDR (auto) dnf install -y rtl-sdr

# --- Web & App Testing ---
SKIPFISH (auto) dnf install -y skipfish
VEGA (manual) git clone https://github.com/subgraph/Vega.git
WEBSCARAB (manual) git clone https://github.com/OWASP/OWASP-WebScarab.git
WFUZZ (pip) pip3 install wfuzz

# --- Forensics & Reverse Engineering ---
VOLATILITY (auto) dnf install -y python3-volatility
AUTOPSY (auto) dnf install -y autopsy
STEGOCRACKER (pip) pip3 install stegcracker
EXIFTOOL (auto) dnf install -y perl-Image-ExifTool
GDB (auto) dnf install -y gdb
RADARE2 (auto) dnf install -y radare2
APKTOOL (manual) git clone https://github.com/iBotPeaches/Apktool.git
JD-GUI (manual) wget https://github.com/java-decompiler/jd-gui/releases/latest/download/jd-gui_linux.sh -O /usr/local/bin/jd-gui && chmod +x /usr/local/bin/jd-gui

# --- Cloud & AD ---
CLOUDENUM (pip) pip3 install cloudenum
MICROSOFT365DEFENDER (pip) pip3 install m365-defender
BLOODHOUND-PY (pip) pip3 install bloodhound

# --- Extra Useful Tools ---
HTTRACK (auto) dnf install -y httrack
MEDUSA (auto) dnf install -y medusa
NCRACK (auto) dnf install -y ncrack
P0F (auto) dnf install -y p0f
IKE-SCAN (auto) dnf install -y ike-scan
FERRET (manual) git clone https://github.com/dbalmain/ferret.git
SPARTA (manual) git clone https://github.com/SECFORCE/sparta.git
BDFPROXY (manual) git clone https://github.com/secretsquirrel/BDFProxy.git
MAGICTREE (manual) git clone https://github.com/504ensicsLabs/MagicTree.git
REGRIPPER (manual) git clone https://github.com/keydet89/RegRipper3.0.git
METAGOOFIL (manual) git clone https://github.com/laramies/metagoofil.git

# --- Python Tools ---
PIP-UPGRADE (pip) pip3 install --upgrade pip
PIP-REQUESTS (pip) pip3 install requests
SHODAN (pip) pip3 install shodan
AUTOPWN (pip) pip3 install autopwn
TERMINETER (pip) pip3 install termineter

# --- Red Team & Post Exploitation ---
POWERSPLOIT (manual) git clone https://github.com/PowerShellMafia/PowerSploit.git
CREDCRACK (manual) git clone https://github.com/gojhonny/CredCrack.git
MIMIKATZ (manual) git clone https://github.com/gentilkiwi/mimikatz.git

# --- End of List ---
FINISHED! (...) echo "Thank You For Using Pentest-Tools-Install."
exit
